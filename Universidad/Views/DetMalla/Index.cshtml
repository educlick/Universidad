@model IEnumerable<Dominio.DetMalla>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MallaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MateriaId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MallaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MateriaId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DetmallaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DetmallaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DetmallaId })
        </td>
    </tr>
}

</table>
