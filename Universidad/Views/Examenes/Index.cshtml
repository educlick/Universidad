@model IEnumerable<Dominio.Examenes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HorarioExamenes.HorarioExamenId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InscripcionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HorarioExamenes.HorarioExamenId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InscripcionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExamenId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExamenId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamenId })
        </td>
    </tr>
}

</table>
