@model IEnumerable<Dominio.Calificaciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Examenes.ExamenId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Calification)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Examenes.ExamenId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Calification)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CalificacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CalificacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CalificacionId })
        </td>
    </tr>
}

</table>
