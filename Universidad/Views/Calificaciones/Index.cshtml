@model IEnumerable<Dominio.Calificaciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Examenes.IdExamen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Calificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Examenes.IdExamen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Calificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCalificaciones }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCalificaciones }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCalificaciones })
        </td>
    </tr>
}

</table>
