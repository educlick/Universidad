@model IEnumerable<Dominio.Malla>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdMalla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomMalla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InicioValidez)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinValidez)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdMalla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomMalla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InicioValidez)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinValidez)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Fecha }) |
            @Html.ActionLink("Details", "Details", new { id=item.Fecha }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Fecha })
        </td>
    </tr>
}

</table>
