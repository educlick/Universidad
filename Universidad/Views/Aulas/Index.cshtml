@model IEnumerable<Dominio.Aulas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoAula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipoAula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAula }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAula }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAula })
        </td>
    </tr>
}

</table>
