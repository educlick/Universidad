@model IEnumerable<Dominio.Correlatividades>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DetMalla.DetmallaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Orden_1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MateriaId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DetMalla.DetmallaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Orden_1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MateriaId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CorredetId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CorredetId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CorredetId })
        </td>
    </tr>
}

</table>
