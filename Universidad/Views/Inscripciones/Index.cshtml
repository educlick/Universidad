@model IEnumerable<Dominio.Inscripciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Alumno.Cedula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Horarios.IdHorario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Matricula.IdMatricula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdMallaAlum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Alumno.Cedula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Horarios.IdHorario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Matricula.IdMatricula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdMallaAlum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdInscripcion }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdInscripcion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdInscripcion })
        </td>
    </tr>
}

</table>
