@model IEnumerable<Dominio.Horarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Aulas.AulaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profesor.Direccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Turnos.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MateriaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cupo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Aulas.AulaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Turnos.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MateriaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cupo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HorarioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HorarioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HorarioId })
        </td>
    </tr>
}

</table>
